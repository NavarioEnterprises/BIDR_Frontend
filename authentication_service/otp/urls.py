"""
URL configuration for OTP module.
"""
from django.urls import path
from . import views

app_name = 'otp'

urlpatterns = [
    # Add OTP-related URL patterns here
    # For now, this is empty to prevent import errors
]
